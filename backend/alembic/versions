# FILE: /FamilyTree/FamilyTree/backend/alembic/versions

# This directory will contain migration scripts generated by Alembic.
# Each migration script will be named with a unique identifier and a brief description of the changes made.
# To create a new migration, use the command:
# alembic revision -m "description of the migration"
# Ensure that the migration scripts are properly documented and tested before applying them to the database.